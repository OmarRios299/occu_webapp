// Tipos compartidos (placeholder).
// Regla: si se genera un contrato TypeScript desde `docs/api-contracts/*`,
// se exporta desde aquí para consumirlo en web-cliente y web-panel.

export type UserNivel = 'Cliente' | 'Propietario' | 'Administrador'

